import React from 'react';
import Base from '../Base';

export default class ProfDumb extends Base {
    constructor(props) {
      super(props);
    }
    render() {
      return (
        <div>hello world</div>
      );
    }
}
